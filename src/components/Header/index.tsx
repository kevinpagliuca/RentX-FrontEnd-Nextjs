import React from "react";
import { XLogoIcon } from "../../assets/Icons";

import { HeaderContainer } from "./styles";

export const Header = () => {
  return (
    <HeaderContainer>
        <button>
          <XLogoIcon />
        </button>
    </HeaderContainer>
  );
};
