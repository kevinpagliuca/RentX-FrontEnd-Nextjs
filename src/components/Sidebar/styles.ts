import styled from "styled-components";

export const SidebarContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  max-width: 5rem;
  height: 100vh;
  position: sticky;
  top: 5rem;
  z-index: 10;
  background: var(--dark);
`;
